// Ques 15   Consider a class named "Stack" that holds a list of objects and has the following operations associated:
//              POP - Pops the last element off the stack
//              PUSH - Pushes an element on top of the stack
//              TOP - Returns the element at the top of the list Implement the aforesaid class

List list=[1,2,3,4,5,6,7,8,9];
list.push(10)
println(list)
list.pop()
println(list)
list.head()
println(list)
list.first()
println(list)